- amazon-scrapper
  a puppeteer based api which fetches you top 5 results on any search keyword.

- instructions
  `npm install`
  `node app.js` inside the directory.
  then run and you're good to go.

- api endpoint
  localhost:3000/search?keyword=gaming+laptops
